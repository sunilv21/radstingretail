# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env
.env*.local
!.env.example

# Common ignores
node_modules
.next/
.DS_Store

# Persisted dev database — anchored to repo root only. Without the leading
# slash this would also ignore server/data/, which holds the HSN master we
# need at build time on Vercel.
/data/